# secrets
.env
.env.test
token_cache.bin

# local memory / notes — keep off GitHub
memory/
MEMORY.md

# project instructions — keep local, not on GitHub
CLAUDE.md

# python
venv/
__pycache__/
*.pyc

# output
briefings/
sarus.log

# meeting-prep dedup state — local, per-machine
prepped.json

# schedule — machine-specific absolute paths, generated locally
com.sarus.briefing.plist
com.sarus.prep.plist
